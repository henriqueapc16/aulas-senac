#variavel
soma = 0
for calculo in range (10): # laço de repetição
  numero = int(input(f"digite um número:")) # input para o usuario digita
  soma += numero  # chamando a variavel soma e numero para realizar a soma, o operador (+=) atribui um valor ao operando à sua esquerda baseado no valor do operando à direita
print("numero",soma)  # imprimir na tela o resultado.

# concluido.

